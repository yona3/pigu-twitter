{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "serve": "yarn build && firebase emulators:start --only functions",
    "shell": "yarn build && firebase functions:shell",
    "start": "yarn shell",
    "deploy": "firebase deploy --only functions",
    "deploy:auto_tweet": "firebase deploy --only functions:auto_tweet",
    "deploy:auto_reserve": "firebase deploy --only functions:auto_reserve",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "14"
  },
  "main": "lib/index.js",
  "dependencies": {
    "dayjs": "^1.10.6",
    "firebase-admin": "^9.11.1",
    "firebase-functions": "^3.15.4",
    "twitter-api-v2": "^1.3.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^0.2.0",
    "typescript": "^3.8.0"
  },
  "private": true
}
